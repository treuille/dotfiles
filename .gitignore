# This is where we store the original dotfiles
original_dotfiles

# Some random crap zsh puts into .config
.config/zsh/.zsh_sessions
.config/zsh/.zsh_history

# Some random crap zsh puts into .config
.config/tmux/plugins

# Lazygit auto-creates its own install folder
.config/lazygit

# We don't care about htop's auto-generated config
.config/htop/htoprc

# GitHub Copilot stuff
.config/github-copilot

# The virtual environment used for installation script
setup/venv
setup/backup
setup/__pycache__
