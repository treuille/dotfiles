# This is where we store the original dotfiles
original_dotfiles

# Some random crap zsh puts into .config
.config/zsh/.zsh_sessions
.config/zsh/.zsh_history

# Some random crap zsh puts into .config
.config/tmux/plugins

# Lazygit auto-creates its own install folder
.config/lazygit

# We don't care about htop's auto-generated config
.config/htop/htoprc

# GitHub Copilot stuff
.config/github-copilot

# btop - a prettier way of getting system stats
.config/btop

# fish (shell, I guess?) - snuck in here when I installed uv
.config/fish

# uv.. a better way to handle python dependencies and venv
.config/uv

# Claude - ignore everything except settings.json
.config/claude/*
!.config/claude/settings.json

# Claude - local settings overrides
.claude

# The virtual environment used for installation script
setup/venv
setup/backup
setup/__pycache__
